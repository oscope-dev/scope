apiVersion: scope.github.com/v1alpha
kind: ScopeDoctorGroup
metadata:
  name: setup
  description: Run dep install
spec:
  actions:
    - name: install
      check:
        paths:
          - '**/requirements.txt'
      fix:
        commands:
          - 'true'
